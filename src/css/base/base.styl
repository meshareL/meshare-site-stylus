
* { box-sizing: border-box; }

body
    font-family $body-font
    -webkit-font-smoothing antialiased
    -moz-osx-font-smoothing grayscale
    font-size $body-font-size
    line-height $body-line-height
    color $text-gray-dark
    background-color $bg-white

a
    color $text-blue
    text-decoration none
    &:hover
        text-decoration underline

b, strong { font-weight: $font-weight-bold; }

button { border-radius: 0; }

input, textarea, select, button
    font-size inherit
    font-family inherit
    line-height inherit

// 删除取消键
[type="search"]::-webkit-search-cancel-button
    appearance none

table
    border-spacing 0
    border-collapse collapse

th, th { padding: 0; }

[hidden][hidden] { display: none; }

details
    summary { cursor: pointer; }
    &:not([open]) > *:not(summary) { display: none; }

details-menu
details-dialog
auto-check
image-crop
    display block

/*
 * https://stackoverflow.com/questions/31402576/enable-focus-only-on-keyboard-use-or-tab-press facebook
 * 使用javaScript监听keydown与mousedown
 * 触发mousedown事件时body添加.using-mouse取消元素outline效果
 * 触发keydown事件时body删除.using-mouse使用浏览器原生outline效果
 */
body.using-mouse
    summary:focus
    button:focus
    [class *= 'btn']:focus
        outline none
        box-shadow none